%h1 Типы приказов

%table
  %tr
    %th Наименование
    %th
    %th
    %th

  - @types.each do |type|
    %tr
      %td= type.name
      %td= link_to 'Показать', type
      %td= link_to 'Редактировать', edit_type_path(type)
      %td= link_to 'Удалить', type, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'Новый тип', new_type_path
